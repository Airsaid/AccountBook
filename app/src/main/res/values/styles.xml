<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--下划线默认颜色-->
        <item name="com.github.airsaid.accountbook:colorControlNormal">@color/colorControlNormal</item>
    </style>

    <!-- Splash主题 -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Toolbar 长按弹出Popup主题 -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!--按钮样式-->
    <style name="Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="Button.Default">
        <item name="android:textColor">@color/textWhite</item>
        <item name="com.github.airsaid.accountbook:backgroundTint">@color/colorAccent</item>
    </style>
    <style name="Button.Default.Margin">
        <item name="android:layout_margin">@dimen/margin</item>
    </style>


</resources>
